"use strict";

let cadenaDeDado = [];

function tirarDado() {
  let numDado = Math.round(Math.random() * (1, 5) + 1);
  console.log(numDado);

  for (let i = 1; i <= sumDado; i++) {
    cadenaDeDado.push(numDado);
    console.log(cadenaDeDado);
  }
}
tirarDado(1, 6);

for (let i = 1; i <= sumDado; i++) {
  cadenaDeDado.push(numDado);
  console.log(cadenaDeDado);
}
